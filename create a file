#include <stdio.h>
int main(){
   FILE * file;
   if (file = fopen("hai.txt", "w")){
      printf("File is creatd ");
      
      fprintf(file,"hai i ",);
      printf("done\n");
   }
   else
   printf("Error");
   fclose(file);
   return 0;
}
